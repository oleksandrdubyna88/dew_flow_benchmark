# research/ — the system as it is

This folder holds **documentation of what exists**: architecture, module deep-dives, and the design
records of decisions that already shipped — including plans whose work is done, kept because the code
references them and they explain why the system looks the way it does.

Open work lives in [todo/](../todo/README.md). The test is a single question:

> Is someone still supposed to build this?

Yes → `todo/`. No → `research/`.

## Currently here

| Document | What it is |
|---|---|
| [architecture.md](architecture.md) | The system as it is — the layers and why two projects depend on nothing, the measurement tuple, one leg end to end as a sequence, the two vantage points on a tool call, the guards that shape the API, and an explicit list of what does NOT exist yet |
| [SPIKE_dotnet_eval_library.md](SPIKE_dotnet_eval_library.md) | Whether `Microsoft.Extensions.AI.Evaluation` could carry this benchmark's scoring, and what it does and does not answer — the record of a question asked before a dependency was taken |
| [MEASURED_LESSONS.md](MEASURED_LESSONS.md) | The evidence base this project is built on — carried over from an earlier measurement programme against a different codebase, so nothing here depends on that repository being checked out. Every guard in the domain traces to a numbered finding in it |
| [PLAN_hardware_sampler.md](PLAN_hardware_sampler.md) | Design record, IMPLEMENTED 2026-08-23 — the machine a number came off. A run records its machine ONCE (five independently-versioned OS layers under WSL, the driver and its health code per adapter, `ProductName` refused because it says *Windows 10* on a Windows 11 box) and each leg records what that machine was doing while it ran, on two clocks: processor and memory at the base tick, VRAM on its own far slower one because a vendor-neutral read costs ~1 s — the plan's own 2-second default, measured and refuted. *Not sampled* is never a zero and a shared card is *observed* rather than *attributed*. Its last item was believed blocked on cross-run comparison and had not been for three days: the arms table folded runs across machines in silence until `MachineAgreement` closed it on 2026-08-23. Open tails, both owned elsewhere: VRAM off Windows, and `Attributed` awaiting the accelerator lease |
| [PLAN_bench_submodule_pin.md](PLAN_bench_submodule_pin.md) | Design record, IMPLEMENTED 2026-08-23 — the daemon stopped building its console out of a path that left its own repository. Four `ProjectReference` lines pointed at `../../../dew_flow_benchmark/`, so a clone of `dew_flow_rag_qln` did not build and two machines could build two different consoles from one qln commit — a reproducibility hole in the build system rather than in a measurement. The blocker was never technical: a submodule records a commit id and this repository's commits had not reached its remote. Written and closed the same day it was extracted from `PLAN_bench_console.md` §5. The real risk was MSBuild's `Directory.Build.props` walk, and it holds — all three `Bench.*` projects build from inside the submodule. Costs one stated thing: the console FREEZES at the pin until somebody bumps it |
| [PLAN_bench_console.md](PLAN_bench_console.md) | Design record, IMPLEMENTED 2026-08-20 — the measurements get a screen, mounted as DewFlow's **Benchmarking** section over `IDaemonModule`, following the `Mcp.Ui` precedent rather than inventing a second mechanism. Tabs are the three KINDS of test (RAG · MCP · Sidecar) with Runs and Arms as views inside a kind; the comparison applies its scope by PARTITIONING rather than refusing, because refusing whenever runs span scopes is correct and useless against a real database. Three refusals that never collapse into one, an undeclared backend excluded and COUNTED, no baseline ever nominated by score — and cost columns beside quality, after three arms scoring zero apiece turned out to cost 2.6 s, 8.7 s and 16.4 s. Deviation: §5's submodule shipped three days late, as a sibling path in the meantime — [PLAN_bench_submodule_pin.md](PLAN_bench_submodule_pin.md) |
| [PLAN_run_report.md](PLAN_run_report.md) | Design record, IMPLEMENTED 2026-08-19 — the comparison comes out of the store. `SeedSplit.Proof`, `Discrimination.Over` and `MetricByDimension.Legs` were written, tested and called by NOTHING: the split that guards against this programme's three reversed conclusions had never once been consulted. Now `bench report` and `bench-api` answer with one object — the metric along four axes, every arm on both halves, `Unproven` printed as a word rather than as a smaller win, a thin mean printing its numbers and withholding the ranking, and no baseline ever nominated by score. Its build found three defects the plan had not predicted: an aggregate hydrating every prompt of a run to average one number, an exit code that called a bad invocation an environment failure, and a split that would have reshuffled at every suite freeze |
| [PLAN_corpus_axis_integrity.md](PLAN_corpus_axis_integrity.md) | Design record, IMPLEMENTED 2026-08-23 — what a chunk axis MEANS, and proving the engine honoured it. A corpus is built by an indexing pass and a search reaches whichever collection the engine resolves, so a variant's chunk size and embedder are CLAIMS until something reads them back — and one measurement was recorded under a variant declaring 512 embed tokens against a 256-token index, every number real and its label wrong. The guard reads the index BEFORE any cell exists and ends the run naming both recipes. Closed here: `chunkTokens` gets its UNIT, so a corpus counted in another model's tokens is refused *even though both sides say 512*. Two axes turned out to be checkable but undeclarable — `EmbedDimensions` had a guard nothing could ever fire, and the tokenizer the engine had reported for a week was read and discarded. Deviation: an unset tokenizer is NOT the refusal §3.1 asked for, because the same parse reads stored rows back. One item is **overtaken**: `bench variants verify` cannot find a mismatch the guard already made impossible. Chunk STRATEGY is extracted, blocked on the engine: `todo/PLAN_chunk_strategy_axis.md` |
| [PLAN_question_bank_coverage.md](PLAN_question_bank_coverage.md) | Design record, IMPLEMENTED 2026-08-23 — the bank went from **22 questions in two groups to 26 accepted across all six**, and the question it opened with was answered by measurement rather than argued: one model in three slots gave 2 rejections in 57 marks, both reproducible by substring arithmetic, while three DIFFERENT families each rejected a class the others were blind to — git history against a seed date, and a compound question that every anchor check had passed. Its most valuable page is a **retraction**: the finding *"two model families both shift seed dates by a day"* was our own timezone arithmetic (a bare `"at"` read as local midnight, normalised to UTC), so both authors had copied correctly and three questions were thrown away over a defect of the harness. Also here: the mechanical gate that saved 45 launches, a reviewer slot that can be retired **and restored** without deleting its marks, and the correction that bank throughput is governed by CLI entitlements rather than machine time. Open tails, none of them a DoD line: gemini has still AUTHORED nothing, and codex is out of quota until 17 September 2026 |
| [PLAN_question_authoring.md](PLAN_question_authoring.md) | Design record, IMPLEMENTED 2026-08-18 — three CLI agents author the bank and three review it. `ICliAgentRuntime` over the one launcher (grown a stdin), a hashed `prompts/` catalog, answers admitted through the bank's EXISTING rules and never repaired, reviewer slots that name their model as data, self-review refused with a cost printed when it is allowed, unanimity as the only promotion rule, and a mechanical gate that stops a launch on a broken anchor or a non-discriminating scoring term. Measured: **17 accepted of 22, ~1.5 min per review, ~7 min per accepted question** — and the panel found nothing the arithmetic could not. Open tail: `todo/PLAN_question_bank_coverage.md` |
| [PLAN_reliability_tail.md](PLAN_reliability_tail.md) | Design record, IMPLEMENTED 2026-08-16 — what the 24/7 audit left after its campaign-ending defects were fixed, and how each remaining item shipped: **one deadline per leg** (`LegDeadline`) so a looping lane cannot multiply a per-call wall by a turn count nobody bounded, budgets **confirmed by the runtime** before a cell exists, both `GetOrAdd`-forever dictionaries bounded, the run summary counted in SQL instead of hydrated, a chunked spool ingest, the `Win32Exception` a best-effort kill used to leak, and `logs/` retention owned by the host at startup |
| [PLAN_tool_telemetry_v0.md](PLAN_tool_telemetry_v0.md) | Design record, IMPLEMENTED 2026-08-15 — the founding plan's §5.4 made concrete: this repository owns the `telemetry/v0` schema, a local spool is the transport, `bench telemetry ingest` is idempotent and resumable, and the §7 AppHost stands up this benchmark's own Postgres. Emitter half: `dew_flow_mcp · research/PLAN_usage_telemetry.md` |

## Conventions

- A plan arrives here only when its work is done, with `> Status: **IMPLEMENTED, <YYYY-MM-DD>.**` and a
  record of what shipped **differently** from the plan. The deviations are the most valuable part.
- Architecture notes follow the `architecture.md` + `module_<name>.md` split once there is enough
  system to describe. Until then this folder holds only what is genuinely settled.
- Cross-repository citations are **paths, not links** (see [todo/README.md](../todo/README.md)): a
  relative link that resolves on one machine is worse than a citation that names its source.

# todo/ — open work

This folder holds **plans for work that is not finished**: proposals, task breakdowns, and implementation
plans that are still open.

Once this repository has documentation of the system *as it is*, that lives in `research/` —
architecture, module deep-dives, and the design records of decisions that already shipped. The convention is
the one used across the `dew_flow_*` family and `claudeRag`.

## The lifecycle

```
new plan  ──►  todo/PLAN_<topic>.md  ──►  implemented  ──►  research/PLAN_<topic>.md
                                                            + research/module_*.md updated
```

When a plan is fully implemented, move it with `git mv`, set its status line to `IMPLEMENTED <date>`, record
what shipped differently from the plan, and fix relative links in both directions. A partially implemented
plan stays where the **majority of its value** lives; its unfinished phases are extracted into a new `todo/`
plan rather than holding the whole document hostage.

## Naming

`PLAN_<snake_case_topic>.md`.

## What every plan carries

- A `> Status:` line on the second or third line — the first thing a reader needs.
- The symptom or goal, stated before any solution.
- Verified references to real code as `file.cs:line` — not guesses.
- A build order, a test plan, and a Definition of Done checklist.

## Related documents

- [../research/README.md](../research/README.md) — what belongs in `research/`, and what is there now.
  Most recently promoted: [PLAN_run_report.md](../research/PLAN_run_report.md) — the comparison comes out
  of the store: four axes, both halves of the split, and `Unproven` printed as a word rather than as a
  smaller win. It closed the third instance of this repository's "built and never called" class, and its
  build turned up three defects the plan had not predicted (2026-08-19). Before it:
  [PLAN_question_authoring.md](../research/PLAN_question_authoring.md) — three CLI
  agents author the bank and three review it; both verbs run live, the throughput is measured, and the
  measurement's own finding is that the panel found nothing the arithmetic could not (2026-08-18). Before
  it: [PLAN_reliability_tail.md](../research/PLAN_reliability_tail.md) — the whole
  tail of the 24/7 audit, from one deadline per leg to a named owner for `logs/` (2026-08-16). Before it:
  [PLAN_tool_telemetry_v0.md](../research/PLAN_tool_telemetry_v0.md) — the §5.4 tool-telemetry contract,
  its spool ingest, and the AppHost that stands up this benchmark's own Postgres (2026-08-15).
- [../research/MEASURED_LESSONS.md](../research/MEASURED_LESSONS.md) — the evidence base the founding
  plan cites. Carried over so no plan here depends on another checkout being present.
- [../CLAUDE.md](../CLAUDE.md) — the project rules; [../.claude/rules/](../.claude/rules/) holds the
  always-loaded and path-scoped ones.

## Cross-repository citations

This repository benchmarks software that lives elsewhere, so its plans cite other checkouts. Those citations
are written as **paths, not links** — `DewFlow · research/RESULTS_rag_eval_v3.md:1111-1114` — because a
relative link that only resolves on one machine is worse than a citation that names its source. `DewFlow ·`
means the `claudeRag` repository, where the measurement history that justifies this project lives.

## Currently open

| Plan | Status |
|---|---|
| [PLAN_rag_bench_repo.md](PLAN_rag_bench_repo.md) | **plan only, 2026-08-14** — the founding plan for this repository. A benchmark for **any repository at any commit, measured by any engine**: target is `(repoUrl, commitSha, exclusions)`, the engine is a parameter (ours, mindex, any HTTP service, and *no retrieval at all* as a first-class engine), suites are frozen and hashed with commit-scoped ground truth, and results are only comparable inside one measurement tuple. Two out-of-band metric modules — a hardware sampler with runs serialised on the accelerator, and a trace port with **both** black-box and white-box implementations, the latter carrying the retrieval funnel. Time in three buckets (tools · thinking · infrastructure wait). CLI first and shaped for an agent, API alongside, UI last in an RCL. Carries a §2 table of nine lessons already paid for in wrong numbers and near-misses, as specification rather than as copied code |
| [PLAN_variant_matrix.md](PLAN_variant_matrix.md) | **steps 1–3 (the variant catalog, the question bank, the model registry) implemented 2026-08-16; step 4 implemented 2026-08-17 — single-shot RAG per cell, funnel + hits + thinking persisted, retrieval metrics real, `bench prune` owning the growth, and the query-time axes wired whole (`fusion=rrf|wsum`, `wsumNorm`, `textShape`) once the sibling plan's step 1 landed; the corpus axes (chunk size, embed model) are not request axes at all and wait for step 5. Steps 5–11 open** — the variant matrix: a Postgres question bank in five named groups (~100 questions each) with per-reviewer marks (`claude`/`codex`/`gemini`, extensible as data); retrieval variants as an immutable catalog so the engine configuration becomes a matrix axis beside subject and repeat; a test = frozen question selection × **all active variants** at a pinned commit, whose completion is derived — adding a variant reopens a finished test for exactly the new cells; every cell runs the full pipeline (retrieve → ask → score → multi-arbiter judge) with funnel, hits, thinking and sampling-as-sent persisted publication-safe; a library-first console (`Bench.Ui` RCL + thin `hosts/Web` shell in the existing AppHost, API-first as a gate); a model registry with per-test subject and ordered-arbiter roles (bridge-local included); an agent lane — cloud CLIs with native tools plus our MCP server. Engine half: `dew_flow_rag_qln · todo/PLAN_search_variant_axes.md` |
| [PLAN_question_bank_coverage.md](PLAN_question_bank_coverage.md) | **partly implemented, 2026-08-19; open work remains** — the author's brief now carries the target's git history, the panel is three DIFFERENT models, the mechanical gate and the seed-date derivation ship, and a reviewer slot can finally be retired (one slot bound to a model that never answered had frozen the entire bank, and nothing could undo an insert). Retracted on 2026-08-19: the "both authors shift seed dates by a day" finding was the bank's own timezone arithmetic — a bare `"at"` read as local midnight and normalised to UTC — so both models had copied correctly and three questions were rejected over a defect of ours; fixed at the boundary in `QuestionSeed.Written`. Still open: the first measurement, `gemini` has authored nothing across three distinct failures, and the five flagged questions. Originally — the bank holds 22 questions in TWO groups and zero in four, which makes every comparison over it a comparison about two groups. Extracted from PLAN_question_authoring when that shipped: the pipeline works and its throughput is measured (17 accepted of 22, ~7 min per accepted question), so what remains is coverage, not machinery. `semantic-intent` and `adversarial` were emptied by the workspace-trust gate that is now fixed and were never retried; `pr-diff` cannot be authored at all until the author gets a tree whose git history it can read (recommendation: put `git log` in the brief rather than change the checkout mode); `code-writing` belongs to PLAN_code_lane. And the panel needs a SECOND MODEL rather than a fourth slot — three slots on one model produced 2 rejections in 57 marks, both reproducible by substring arithmetic, and approved one question carrying the same defect class it rejected another for |
| [PLAN_code_lane.md](PLAN_code_lane.md) | **plan only, 2026-08-16** — group 6 of the bank: tasks a model must SOLVE (fix a live bug · implement a stated TODO), because reading saturated at 89–100 %. Authoring is a model's job with three gates (the bug reproduces, the reference fix works, the tree is rebuilt to the buggy state) and the **reviewers write the hidden tests**; solving runs Investigate → Implement → Verify → Judge with per-phase budgets in a per-leg sandbox worktree. Scored by two independent instruments: five mechanical signals (compiles · right file · hidden tests green · the solver's own test has teeth · neighbours intact) and an inflation-resistant delivered-work score ported — mechanism, not repository — from `scoreMeter` V2, whose zero band took 160 of 160 padded steps and made ×10 padding buy ×0.88. Its 13.2 % sampling spread is why the code lane repeats ≥ 2 |
| [PLAN_scoremeter_port.md](PLAN_scoremeter_port.md) | **plan only, 2026-08-16** — three things ported from the operator's `scoreMeter` V2, as ONE independent leaf module (`src/Bench.Delivered`, zero references, architecture-tested): the cleaned-LOC family (case-sensitive first-match-wins path fates, line normalization with continuation joining — both adapted for C#: `#if` is not a comment, obj/bin/Designer/g.cs get fates), the zero-band anchored weighting protocol (×10 padding bought ×0.88 in the source's measured arms; 160/160 padded steps zeroed) with coverage gate and deterministic policy over raw scores (raw + applied + rule persisted, `bench rescore` recomputes with zero model calls), and the frozen-arms measurement method — parity fixtures against published numbers, all inherited constants named with their source, an *inherited calibration* badge until the inflation property is re-verified on this corpus. Grain is deliberately NOT ported |
| [PLAN_tool_benchmark.md](PLAN_tool_benchmark.md) | **step 1 implemented 2026-08-19** — the lane catalog: a lane is a hashed, immutable row (tool subset · description set · doctrine · presentation · turn ceiling) rather than a bare name, with `bench lanes add|list|retire` and eight tests against a real Postgres. `Lane.Preamble`, declared on day one and read by nothing, is finally the doctrine. Steps 2–4 and 6–11 open; step 5 shipped in `dew_flow_mcp`. Originally: **plan only, 2026-08-16** — the tool benchmark: measuring the tools we hand a model, and the words we hand them with. A lane becomes a catalog row like a variant (tool subset · description set · **doctrine** · presentation · turn ceiling), immutable and hashed, with no schema change to `cells`; `Lane.Preamble` — declared in the founding tuple and read by nothing — is revived as the doctrine axis, because rewriting one ordering instruction moved a score **16.5 points of 63** where swapping 4 tools for 18 moved **1**. Adds the tool-calling loop the domain has been describing since `BudgetKind.Turns` (`Turns` confirmed by the loop, exhaustion settles `CapExceeded`), two expectation kinds (`ToolUsed`/`ToolNotUsed`) scored with the same *not applicable* fairness rule anchor recall already uses, and a three-rung ladder where L1 and L2 are one mechanism at two turn ceilings. The surface is **echoed, never assumed**; observed and telemetry-reconstructed tool calls are stored side by side and never averaged together. Vendors `dew_flow_mcp` as a submodule so the in-process bridge — the arm that scored 36/63 where its wire twin scored 4/63 — is measured genuinely in-process. CLI → API → UI. Sibling half: `dew_flow_mcp · research/PLAN_tool_surface_config.md` |
| [PLAN_corpus_axis_integrity.md](PLAN_corpus_axis_integrity.md) | **plan only, 2026-08-16** — what a chunk axis MEANS, and proving the engine honoured it. `CorpusSpec` gains `Strategy` (structural against flat-window) and `Tokenizer`, both in the canonical form: `chunkTokens` is an `int` with no unit today, and `256` under two models' tokenizers is two different amounts of text hashed as one comparable configuration. The guard is the point — an engine echoes the corpus recipe it actually served, and a cell whose echo differs from its variant is **blocked with both recipes named**, because today two variants differing only in chunk size resolve to one collection (the engine derives its window from the model) and would be reported as a comparison of nothing. Three states, never two: matched · mismatched · not declared. Then the axis becomes turnable — CLI `--strategy`/`--tokenizer` and `bench variants verify`, API, and UI pages last. Engine halves: `dew_flow_rag_qln · todo/PLAN_tokenizer_contract_and_chunk_coverage.md` and its `PLAN_search_variant_axes.md` §3.2–3.3 |
| [PLAN_compute_backend_axis.md](PLAN_compute_backend_axis.md) | **plan only in this repository, corrected 2026-08-19 — the toolchain blocker is SATISFIED and 2 of 5 arms are MEASURED, outside this harness.** ONNX Runtime 1.24.4 `--use_migraphx` has been at `/opt` since 2026-07-27 and the WSL sidecar was built 2026-08-18, so the §6 blocker had expired three days before anyone re-read it; arms D and W were then run and written up in `dew_flow_rag_qln · research/GPU_BACKEND_WSL_VS_WINDOWS.md` (per token MIGraphX ~1.5× faster, per pass a tie, reconciled by our own `ChunkBatcher`). Outstanding: arms I · C₁ · C₂ — the CPU control pair is the only evidence about the operating system itself, so nothing is published until they run — and **phase 2, the echo (§5a), which is what decides whether those numbers can live in this benchmark at all**: two engines differing only in their sidecar are still one result row. Phase 3's blocker is gone (matrix step 4 landed 2026-08-17); it is gated on phase 2 alone. Originally — the compute backend becomes an axis, and its first question is the operator's: on one R9700, is the sidecar faster under WSL (ROCm/MIGraphX) or on Windows (DirectML)? Two corrections shape the plan. **The host and the EP cannot be separated**: MIGraphX exists only under Linux/WSL (no prebuilt ORT ships it; the library is built from source in the distro) and DirectML only on Windows, so the comparison is unavoidably both at once — a CPU arm on each host is the ONLY evidence about the operating system itself, and no column may be labelled by host alone. And **the mixed WSL+Windows topology exists and was the DEFAULT for two days**: embed in WSL/MIGraphX on the discrete card, a separate Windows/DirectML sidecar serving `/rerank` on the iGPU (`RerankSidecarPlan` was opt-OUT until `DewFlow · 47235cd0`); what has never existed is a mixed *embed fleet*. "Faster" is then answered three times, never once — steady state, cold start, shape churn — because MIGraphX compiles **per input shape** (2–4 min, ~2.5 GB apiece) while DirectML on this exact card held 30.6 GB of 32 and removed the D3D12 device three times in an hour. Instrument: the sidecar's own `PassTimings` split, whose compile field became trustworthy only on 2026-08-16 when it was scoped per engine. Structurally, two engines differing only in their sidecar are indistinguishable in a result row today (`engine = kind·endpoint·version·indexFingerprint`), so the engine must **echo** the backend it served on and a mismatch must block the cell — the discipline of `PLAN_corpus_axis_integrity`. The WSL arms want ONNX Runtime 1.24.x built `--use_migraphx`, the same blocker as `dew_flow_sidecar_rust · research/PLAN_reliability_tail.md` item 1 |
| [PLAN_corpus_litter.md](PLAN_corpus_litter.md) | **plan only, 2026-08-15** — a benchmark run must not leave a corpus behind. Raised from a measurement in `dew_flow_rag_qln`: Qdrant held 24.38 GB of which **22 GB was rubbish**, nineteen collections left by test runs that minted a fresh project id each time. One aspnetcore corpus is ~2 GB and a 24-variant matrix is ~1.2 M points, so a sweep that leaks one per cell fills a disk in an afternoon — and reports it as "no space" during an unrelated run. Proposes a `bench_` namespace so "ours" is decidable from the name, delete-on-finish including the failure path, and a retention listing with a button rather than a silent timed sweep |

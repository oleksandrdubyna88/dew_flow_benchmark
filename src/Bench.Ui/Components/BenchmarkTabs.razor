@* The section's tabs: the KINDS of test this harness runs, not views of one of them.

   Runs and arms live INSIDE a kind, because they mean different things in each: an arm is a compute
   backend for the sidecar question and a tool lane for the MCP one, and one flat list of runs across all
   three would be a list nobody can read a comparison out of.

   RAG and MCP are declared here before they are built, deliberately: the structure is the claim about what
   this section is for, and a tab that appears later reads as a feature nobody planned. Each says what will
   live there rather than rendering an empty page. *@

<ul class="nav nav-tabs mb-3">
    <li class="nav-item">
        <NavLink class="nav-link" href="/benchmarking/rag" Match="NavLinkMatch.All">RAG</NavLink>
    </li>
    <li class="nav-item">
        <NavLink class="nav-link" href="/benchmarking/mcp" Match="NavLinkMatch.All">MCP</NavLink>
    </li>
    <li class="nav-item">
        <NavLink class="nav-link" href="/benchmarking/sidecar">Sidecar</NavLink>
    </li>
    <li class="nav-item">
        @* The code kind (`PLAN_code_lane.md` §7 step 8): fix tasks a model investigates and solves. Its
           runs used to land on the Sidecar tab's stopgap list, where an investigate run read as a
           hardware comparison — caught by the operator 2026-08-21. *@
        <NavLink class="nav-link" href="/benchmarking/code" Match="NavLinkMatch.All">Code</NavLink>
    </li>
    <li class="nav-item">
        @* Math (`todo/ai_math/PLAN_math_over_ai.md`): the INVESTIGATE tab. Its population is not runs at
           all — it is real agent sessions, traced deterministically, read for the steps a formula could do
           instead. It sits beside the other kinds because it answers the same shape of question about a
           different subject: not "which configuration is better" but "which of this work did not need a
           model". *@
        <NavLink class="nav-link" href="/benchmarking/math" Match="NavLinkMatch.All">Math</NavLink>
    </li>
</ul>

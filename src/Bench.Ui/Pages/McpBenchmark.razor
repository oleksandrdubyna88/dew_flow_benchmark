@page "/benchmarking/mcp"

<PageTitle>Benchmarking · MCP</PageTitle>

<h1 class="h3 mb-3">Benchmarking</h1>
<BenchmarkTabs />

<PendingKind Kind="MCP"
             Will="The tools we hand a model, and the words we hand them with — lanes as hashed catalog rows (tool subset, description set, doctrine, turn ceiling), scored by tool use rather than by answer alone. Rewriting one ordering instruction moved a score 16.5 points of 63 where swapping 4 tools for 18 moved 1, which is why the doctrine is an axis."
             Plan="todo/PLAN_tool_benchmark.md" />
